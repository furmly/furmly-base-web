import styled from "styled-components";
import { errorColor, smallestText } from "../variables";
export default styled.p`
  color: ${errorColor};
  font-size: ${smallestText}px;
  font-weight: bold;
  margin: 1px 0px;
  display: block;
`;
