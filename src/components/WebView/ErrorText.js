import styled from "styled-components";
import ErrorText from "../common/components/ErrorText";
import { titleText } from "../common/variables";

export default styled(ErrorText)`
  font-size: ${titleText};
`;
